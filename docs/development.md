# Development

Use short-lived branches from `main` for implementation work.

- Keep changes focused.
- Add or update tests with behavior changes.
- Open pull requests against `main`.
- Use `develop` for environment checks that should not affect the baseline.
