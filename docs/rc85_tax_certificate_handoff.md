# RC85 tax certificate handoff

RC85 moved certificate rows onto route-scoped invoice output. A count-only smoke passed, but one partner sample still disagrees on whether a zero-cent exemption with a certificate should remain exempt.

Treat broad tax-policy cleanup as separate unless the code path affects the release artifact sample.
